import React from 'react';

export default function NotFound (props) {
    console.log(props);
    return (
        <section>
            <h5>404 Not Found</h5>
        </section>
    );
}

export default NotFound;